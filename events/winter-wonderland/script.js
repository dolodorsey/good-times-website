const SUPABASE_URL='https://dzlmtvodpyhetvektfuo.supabase.co';const SUPABASE_KEY='sb_publishable_ekvoOK6QQ05dUZuWgzQfUw_2RgbWPFR';const slug='winter-wonderland';const q=s=>document.querySelector(s);
document.addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--x',e.clientX+'px');document.documentElement.style.setProperty('--y',e.clientY+'px')},{passive:true});const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('on')),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
async function loadSettings(){try{const r=await fetch(`${SUPABASE_URL}/rest/v1/event_site_settings?event_slug=eq.${slug}&select=*`,{headers:{apikey:SUPABASE_KEY}});const [d]=await r.json();if(!d)return;q('#eyebrow').textContent=d.eyebrow||q('#eyebrow').textContent;q('#subheadline').textContent=d.subheadline||q('#subheadline').textContent;q('#dateLabel').textContent=d.date_label||'Winter 2026';q('#season').textContent=d.date_label||'Winter 2026';q('#doorsTime').textContent=d.doors_time||'TBA';q('#city').textContent=d.city||'Atlanta, GA';q('#venueName').textContent=d.venue_name||'Private location';if(d.metadata?.dress_code)q('#dressCode').textContent=d.metadata.dress_code}catch(e){console.warn('Using launch placeholders')}}
q('#leadForm').addEventListener('submit',async e=>{e.preventDefault();const btn=e.currentTarget.querySelector('button');const status=q('#formStatus');const fd=new FormData(e.currentTarget);btn.disabled=true;btn.textContent='Freezing your access…';status.textContent='';const p=new URLSearchParams(location.search);const body={event_slug:slug,first_name:fd.get('first_name'),last_name:fd.get('last_name')||null,email:fd.get('email'),phone:fd.get('phone')||null,interest_type:fd.get('interest_type'),party_size:Number(fd.get('party_size')||1),website:fd.get('website')||'',source_url:location.href,utm_source:p.get('utm_source'),utm_medium:p.get('utm_medium'),utm_campaign:p.get('utm_campaign'),consent_marketing:true};try{const r=await fetch(`${SUPABASE_URL}/rest/v1/event_site_leads`,{method:'POST',headers:{apikey:SUPABASE_KEY,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());status.textContent='You are inside the Winter Wonderland first-access list.';e.currentTarget.reset()}catch(err){status.textContent='Your request did not save. Please retry in a moment.'}finally{btn.disabled=false;btn.textContent='Enter the Winter Wonderland list'}});
const c=q('#snow'),ctx=c.getContext('2d');let flakes=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);flakes=Array.from({length:Math.min(180,Math.floor(innerWidth/7))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*2.8+.4,v:Math.random()*.75+.2,w:Math.random()*1.2-.6,a:Math.random()*.65+.25}))}function snow(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const f of flakes){f.y+=f.v;f.x+=f.w;if(f.y>innerHeight+5){f.y=-5;f.x=Math.random()*innerWidth}ctx.beginPath();ctx.fillStyle=`rgba(235,253,255,${f.a})`;ctx.arc(f.x,f.y,f.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(snow)}addEventListener('resize',resize);resize();snow();loadSettings();
